import AppBar from 'react-toolbox/lib/app_bar';
import React from 'react';

const LabAppBar = (props) => (
	<AppBar title='P3202, Буланцов, Ощепков, вариант, 4984561'>
		{props.children}
	</AppBar>
);

export default LabAppBar;
